# Hierarchy + process map — LAB-CMS-001 (Builder)

Draw or list one chosen local experience:

```text
Human feel (stutter / hang / heat)
  → App UI
    → Process / threads (app state)
      → OS scheduler + memory manager + file I/O
        → CPU activity
        → RAM (volatile working set)
        → Persistent storage (files / SSD / disk)
        → Power / thermal limits (qualitative unless measured)
```

## Variable changed (one only)

- Variable:
- Why this variable:
- Re-run result (observation only):

## Personal hierarchy notes

(What moved? What stayed flat? What remains unknown?)
