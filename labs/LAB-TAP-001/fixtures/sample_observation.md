# Sample observation (fixture)

**Route:** supplied fixture (no live network required)

## Observation

Local-only markers completed in a few milliseconds on the fixture clock. The network-dependent path waited about one second before response markers appeared.

## Inference

The remote delay is consistent with an intentional demo delay service, not proof of a user's home network quality.

## Limits

This fixture is illustrative teaching data, not a device benchmark.
